---
title: "Prediting laptop prices"
subtitle: ""
author: "Code Crunchin <br> Ben, Adam, Gandom, Jamie"
institute: "University of Edinburgh"
date: "`r Sys.Date()`"
output:
  xaringan::moon_reader:
    css: xaringan-themer.css
    lib_dir: libs
    nature:
      ratio: "16:9"
      highlightStyle: github
      highlightLines: true
      countIncrementalSlides: false
      
---

```{r install-package, include = FALSE, eval = FALSE}
# Copy and paste the following code into your console to download and install
# the `xaringan` package that contains all of the code which allows you 
# to create presentation slides in Rmarkdown
install.packages('xaringan')
install.packages("plotly")
```


```{r load-packages, include = FALSE}
# Add any additional packages you need to this chunk
library(tidyverse)
library(tidymodels)
library(palmerpenguins)
library(knitr)
library(xaringanthemer)
library(plotly)
```

```{r setup, include=FALSE}
# For better figure resolution
knitr::opts_chunk$set(fig.retina = 3, dpi = 300, fig.width = 6, fig.asp = 0.618, out.width = "80%")
```

```{r load-data, include=FALSE}

laptop_data <- read_csv("data/laptop_data_cleaned.csv")
laptop_data_final <- read_csv("data/laptop_data_final.csv")

```

```{r include=FALSE}

#Background image
style_xaringan(
  title_slide_background_image = ""
)
```

class: center, middle

## To try and predict the price of a laptop based on its features and specifications.

---

class: inverse, center, middle

# Background Information

---

# The What?

- The dataset we are analysing is comprised of the specifications of various laptops, obtained from kaggle, updated most recently a year ago.

--

# Glossary 

- Hard Disk Drive and Solid State Drive: A both types of long term storage for computers, Hard disk drives are generally slower and less reliable and Solid state drives are newer, faster and common place is newer computers

- RAM: Random access memory: Short term memory, where your computer stores your project or homework in R before you save it.

- CPU and GPU- Both processors, generally decides how fast your computer can load things, cpus are more general with gpus being specialized for graphics 


---
class: inverse, middle, center

# Data Cleaning and Wrangling

---

# Data Cleaning 

- Luckily the data was in a generally cleaned state already, we merged the data with a previous uncleaned form and extracted the variable processor speed.

- We also found that the price variable was not only in rupees but in natural log form.

- Fortunately, The data had no missing values however we removed laptops with 0 storage, as this is not possible.

- We also decided to only consider laptops from the top 5 companies, otherwise the model would be very difficult to interpret in any meaningful way.
---
class: inverse, center, middle

# Data Visualising
---
```{r correlation matrix plot, echo=FALSE}
correlation_matrix <- laptop_data_final %>%
  select_if(is.numeric) %>%
  cor(use = "complete.obs")


cor_tidy <- as.data.frame(as.table(correlation_matrix)) %>%
  rename(Var1 = Var1, Var2 = Var2, correlation = Freq)

cor_tidier<-cor_tidy%>%
  arrange(,correlation)

cor_tidier%>%
  filter(Var2=="log_price",Var1!="Price",Var1!="log_price")%>%
ggplot( aes(x = Var1, y = Var2, fill = correlation)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(correlation, digits = 2)))+
  scale_fill_gradient2(low = "navy", mid = "white", high = "red", midpoint = 0) +
  labs(title = "Correlation Matrix", x = "Natural log of Price", y = "") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))


```
---
```{r Ram vs log_price, echo=FALSE}

laptop_data_final %>%
  ggplot(mapping = aes(x=Ram,y=log_price,)) +
  geom_point(color="navy") +
  geom_smooth(, method = "lm", color="orange" )+
  labs(
    title = "RAM against ln(Price)",
    x = "Ram",
    y = "ln(Price)",
  )

```
---
```{r log_price vs SSD, echo=FALSE}
laptop_data_final %>%
  ggplot(mapping = aes(x=SSD,y=log_price))+
  geom_point(color="navy") +
  geom_smooth(, method = "lm", color="orange" )+
  labs(
    title = "SSD against ln(Price)",
    x = "SSD(GB)",
    y = "ln(Price)",
  )
```
---
```{r log_price vs SSD vs Ram, echo=FALSE}
p <- plot_ly(laptop_data_final,
  x = ~Ram, y = ~SSD, z = ~log_price,
  marker = list(size = 3, color = "orange", alpha = 0.5, 
                line = list(color = "blue", width = 2))) %>%
  add_markers() %>%
  plotly::layout(scene = list(
    xaxis = list(title = "Ram"),
    yaxis = list(title = "SSD "),
    zaxis = list(title = "log_price"),
    camera = list(eye = list(x = 2, y = 2, z = 2))
  ))

p
```
---
```{r silly faceting, echo =FALSE}

laptop_data_long <- laptop_data_final %>%
  mutate(Ips = as.character(Ips), 
         TouchScreen = as.character(TouchScreen))%>%
  pivot_longer(cols = c(Company, TypeName,TouchScreen,Ips,Cpu_brand,Gpu_brand,Os,storage_type), names_to = "x_var", values_to = "x")

ggplot(laptop_data_long, aes(x = x, y = log_price))+
  geom_bar(stat = "identity", position = "dodge")+
  facet_wrap(~x_var, scales = "free_x")+
  theme_minimal()+
  labs(x = "X Variables",
       y = "Log Price"
       )

```
---

# Layouts

You can use plain text

- or bullet points

.pull-left[
or text in two columns $^*$
]
.pull-right[
- like
- this
]

.footnote[
[*] And add footnotes
]

---

# Code

```{r boring-regression}
# a boring regression
model <- lm(dist ~ speed, data = cars)
tidy(model)
glance(model)
```

---

# Plots

```{r recode-species, echo = FALSE}
# In this chunk I'm doing a bunch of analysis that I don't want to present 
# in my slides. But I need the resulting data frame for a plot I want to present.
iris_modified <- iris %>%
  mutate(Species = fct_other(Species, keep = "setosa"))
```

```{r plot-iris, echo = FALSE}
# Code hidden with echo = FALSE
# Uses modified iris dataset from previous chunk
# Play around with height and width until you're happy with the look
ggplot(data = iris_modified, mapping = aes(x = Sepal.Width, y = Sepal.Length, color = Species)) +
  geom_point() + 
  theme_minimal() # theme options: https://ggplot2.tidyverse.org/reference/ggtheme.html
```

---

## Plot and text

.pull-left[
- Some text
- goes here
]
.pull-right[
```{r warning=FALSE, out.width="100%", fig.width=4, echo=FALSE}
# see how I changed out.width and fig.width from defaults
# to make the figure bigger
ggplot(penguins, aes(x = bill_length_mm, y = species, color = species)) +
  geom_boxplot() +
  theme_minimal()
```
]

---

# Tables

If you want to generate a table, make sure it is in the HTML format (instead of Markdown or other formats), e.g.,

```{r iris-table, echo = FALSE}
kable(head(iris), format = "html")
```

---

# Images

```{r castle, echo = FALSE, out.width = "55%", fig.align = "center", fig.cap = "Image credit: Photo by Jörg Angeli on Unsplash."}
include_graphics("img/edinburgh-castle.jpg")
```

Or you can also include a full page image. See next slide.

---


class: inverse, center, middle
background-image: url(img/edinburgh-castle.jpg)
background-size: contain
---

# Math Expressions

You can write LaTeX math expressions inside a pair of dollar signs, e.g. &#36;\alpha+\beta$ renders $\alpha+\beta$. You can use the display style with double dollar signs:

```
$$\bar{X}=\frac{1}{n}\sum_{i=1}^nX_i$$
```

$$\bar{X}=\frac{1}{n}\sum_{i=1}^nX_i$$

Limitations:

1. The source code of a LaTeX math expression must be in one line, unless it is inside a pair of double dollar signs, in which case the starting `$$` must appear in the very beginning of a line, followed immediately by a non-space character, and the ending `$$` must be at the end of a line, led by a non-space character;

1. There should not be spaces after the opening `$` or before the closing `$`.

1. Math does not work on the title slide (see [#61](https://github.com/yihui/xaringan/issues/61) for a workaround).

---

# Feeling adventurous?

- Want to find out more about `xaringan`? See https://slides.yihui.name/xaringan/#1.

- You are welcome to use the default styling of the slides. In fact, that's what I expect majority of you will do. You will differentiate yourself with the content of your presentation.

- But some of you might want to play around with slide styling. The 
`xaringanthemer` provides some solutions for this that: https://pkg.garrickadenbuie.com/xaringanthemer.

- And if you want more bells and whistles, there is also `xaringanExtra`: https://pkg.garrickadenbuie.com/xaringanExtra.
